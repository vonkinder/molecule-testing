---
# defaults file for hardening_linux

# Configuración de firewall
firewall_service_name: firewalld
firewall_rules:
  - { port: 22, protocol: tcp }
  - { port: 80, protocol: tcp }
  - { port: 443, protocol: tcp }
